#!/bin/bash

set -eu

while read -r line; do
    echo -n "$line [Y/n]"
    read proceed </dev/tty
    if [[ "${proceed,,}" == "n" ]]; then
        continue
    fi
    echo -n "$line" | copy
done < <(cat)
