#!/bin/bash

target="$1"

if [[ ! "$target" ]]; then
    echo "provide target"
    exit 1
fi

while true; do
    if [[ "$(pwd)" == "/" ]]; then
        exit 1
    fi

    if [[ -e "$(pwd)/$target" ]]; then
        pwd
        exit 0
    fi

    cd ../ || exit
done
