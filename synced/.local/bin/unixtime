#!/bin/bash

if [[ "$1" ]]; then
    if [[ "$OSTYPE" == 'darwin'* ]]; then
        converted="$(date -r "$1" -Iseconds)"
    else
        converted="$(date -d @"$1" -Iseconds)"
    fi

    echo "$converted"
    exit
fi

echo "$(date +%s)"
